/* eslint-disable no-unused-vars */
/**
 * List of supported addon types.
 * @export
 * @enum {number}
 */
export enum McpExtensionType {
  /**
   * MCP Connector allows to connect and use tools via Model Context Protocol.
   */
  CONNECTOR = 'mcp-connector',
}
